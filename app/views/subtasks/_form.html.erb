<%= form_for [@assignment,@subtask] do |f| %>
  
  <div class="field">
    <%= f.label :subtask_content %><br />
    <%= f.text_area :subtask_content %>
  </div>
  
  <div class="actions">
    <%= f.submit "Submit" %>
  </div>
<% end %>

