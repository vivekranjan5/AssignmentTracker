<%= form_for @assignment, :url => { :action => "createassignees"}, :html => { :method => "post"} do |f| %>
	<% if @assignment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@assignment.errors.count, "error") %> prohibited this assignment from being saved:</h2>

      <ul>
      <% @assignment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= fields_for (@assignment_user) do |temp_users| %>
         <div class="field">
        <%= temp_users.label "Project Assignees" %>
        <%= collection_select(:users,:id, @all_users, :id, :name, {}, {:multiple => true}) %>
      </div>
    <% end %>
    <div class="actions">
    <%= f.submit "Assign to users"%>
  </div>
<% end %>